# pnpm configuration
# Use hoisted dependencies for better compatibility with Next.js
shamefully-hoist=true

# Auto install peer dependencies
auto-install-peers=true

# Strict peer dependencies (can be disabled if needed)
strict-peer-dependencies=false

# Use node-linker for better compatibility
node-linker=hoisted

# Enable side effects cache
side-effects-cache=true